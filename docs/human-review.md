# Human review

Template nằm tại `review/templates`; hồ sơ làm việc nằm tại `review/inputs`; bằng chứng nằm tại `review/attachments`; kết quả người duyệt trả lại đặt tại `review/returned`. Các thư mục có dữ liệu/corpus đều được loại khỏi Git.

Xem [hướng dẫn bàn giao](offline/human_review_handoff.md) và [các bước tiếp theo](offline/review_next_steps.md).
