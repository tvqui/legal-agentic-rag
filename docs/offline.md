# OFFLINE

OFFLINE nhận corpus trong `data`, sinh kết quả vào `artifacts`, rồi kiểm tra Document Registry, Structured Provisions, Versioned HierarGraph, Dense/BM25 và Neo4j.

Chạy `run.bat offline`; xem báo cáo hiện hành trong `docs/offline/` và `artifacts/reports/`.
